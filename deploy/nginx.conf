server {
    listen 80;
    listen [::]:80;
    
    # Ersetze 'example.com' mit deiner Domain
    server_name example.com www.example.com;
    
    # SSL Configuration (auskommentieren nach dem Zertifikat-Erhalt)
    # return 301 https://$server_name$request_uri;

    root /var/www/mtg-commander-tracker/dist;
    index index.html;

    # Cache für statische Assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # SPA Routing - alle Anfragen an index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# HTTPS Server (aktivieren nach SSL-Einrichtung)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     
#     server_name example.com www.example.com;
#     
#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/example.com/chain.pem;
#     
#     root /var/www/mtg-commander-tracker/dist;
#     index index.html;
#     
#     # SSL Configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#     
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#     
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
